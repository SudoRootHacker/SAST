sonar.sources=.
sonar.tests=tests
sonar.test.inclusions=tests/**/*.php

sonar.exclusions=vendor/**,reports/**,.scannerwork/**,coverage.xml,zap-report.html,**/*.html,tests/**

sonar.php.coverage.reportPaths=coverage.xmlsonar.projectKey=sast-demo
sonar.projectName=sast-demo

# ONLY application code
sonar.sources=.
sonar.exclusions=vendor/**,tests/**,reports/**,.scannerwork/**,coverage.xml,zap-report.html,**/*.html

# Explicit test directory (optional but safe)
sonar.tests=tests

# Coverage
sonar.php.coverage.reportPaths=coverage.xml

sonar.sourceEncoding=UTF-8sonar.projectKey=sast-demo
sonar.projectName=sast-demo
sonar.sources=.

# Only scan source code
sonar.exclusions=vendor/**,reports/**,.scannerwork/**,coverage.xml,zap-report.html,**/*.html

# PHPUnit
sonar.php.coverage.reportPaths=coverage.xml

# Tests
sonar.tests=tests

# Encoding
sonar.sourceEncoding=UTF-8
